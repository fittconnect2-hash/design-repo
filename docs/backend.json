{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the DesignDock application, storing their profile information and serving as a link to an external authentication system like Firebase.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity. This ID typically corresponds to the user's ID from an external authentication system (e.g., Firebase Auth UID)."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "The user's preferred display name."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "createdAt"
      ]
    },
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "description": "Represents a high-level project or category that groups multiple designs.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Project."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this project."
        },
        "name": {
          "type": "string",
          "description": "The name of the project."
        },
        "description": {
          "type": "string",
          "description": "A description of the project."
        },
        "startDate": {
          "type": "string",
          "description": "The start date of the project.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The end date of the project.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the project was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the project was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "description",
        "createdAt"
      ]
    },
    "Design": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Design",
      "type": "object",
      "description": "Represents a design created and managed by a user in the DesignDock application, including details, links, and associated images.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Design entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this design. (Relationship: User 1:N Design)"
        },
        "projectId": {
          "type": "string",
          "description": "Reference to the Project this design belongs to. (Relationship: Project 1:N Design)"
        },
        "name": {
          "type": "string",
          "description": "The name of the design."
        },
        "version": {
          "type": "string",
          "description": "The version of the design, e.g., '1.0'."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the design."
        },
        "figmaLink": {
          "type": "string",
          "description": "URL to the Figma design file for this design.",
          "format": "uri"
        },
        "prototypeUrl": {
          "type": "string",
          "description": "URL to the live prototype of the design.",
          "format": "uri"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL to the associated image for the design.",
          "format": "uri"
        },
        "tags": {
          "type": "array",
          "description": "A list of keywords or tags associated with the design.",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the design was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the design was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "projectId",
        "name",
        "description",
        "version",
        "figmaLink",
        "prototypeUrl",
        "imageUrl",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles. The document ID '{userId}' must match the Firebase Authentication UID for the user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/projects/{projectId}",
        "definition": {
          "entityName": "Project",
          "schema": {
            "$ref": "#/backend/entities/Project"
          },
          "description": "Stores high-level projects, nested under the owning user. The document ID '{projectId}' uniquely identifies a project for that user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user who owns the project."
            },
            {
              "name": "projectId",
              "description": "The unique identifier for the project."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/designs/{designId}",
        "definition": {
          "entityName": "Design",
          "schema": {
            "$ref": "#/backend/entities/Design"
          },
          "description": "Stores individual designs, nested under the owning user. The document ID '{designId}' uniquely identifies a design for that user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user who owns the design."
            },
            {
              "name": "designId",
              "description": "The unique identifier for the design."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure separates Users, Projects, and Designs into distinct collections. User data is at the root under `/users/{userId}`. Both Projects and Designs are stored in subcollections under the user who owns them (`/users/{userId}/projects/{projectId}` and `/users/{userId}/designs/{designId}`). This hierarchical structure ensures that all of a user's data is logically grouped and enables simple, path-based security rules. By storing the `userId` field within every Project and Design document (denormalization), we achieve Authorization Independence, allowing security rules to verify ownership without costly cross-document `get()` calls. This design supports secure queries (QAPs), as a user can only list projects or designs under their own user path, naturally filtering the data by owner."
  }
}
